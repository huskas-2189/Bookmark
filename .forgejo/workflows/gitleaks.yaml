name: Gitleaks

on:
  push:
    branches: [main, develop]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  gitleaks:
    name: "Gitleaks ${{ github.event_name == 'pull_request' && format('PR #{0}', github.event.pull_request.number) || github.ref_name }}"
    runs-on: docker
    container: node:lts-alpine3.23@sha256:0388af2af070cd4736a1567cfed02469ba117848845b4165d87a333edb53d2ca
    steps:
      - name: Install deps
        run: |
          apk add --no-cache \
            git \
            curl \
            tar

      - name: Install Gitleaks
        run: |
          curl -sSL -o gitleaks.tar.gz \
            https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz
          tar -xzf gitleaks.tar.gz gitleaks
          chmod +x gitleaks
          mv gitleaks /usr/local/bin/gitleaks

      - name: Checkout
        uses: actions/checkout@v7

      - name: run Gitleaks
        id: gitleaks_run
        continue-on-error: true
        run: |
          gitleaks dir . --report-format csv --report-path gitleaks-report.csv

      - name: Upload Gitleaks result
        if: steps.gitleaks.outcome == 'failure'
        uses: https://data.forgejo.org/forgejo/upload-artifact@v5
        with:
          name: Gitleaks Report
          path: gitleaks-report.csv

      - name: Fail if secrets found
        if: steps.gitleaks.outcome == 'failure'
        run: |
          echo "Gitleaks detected one or more potential secrets. See the uploaded report."
          exit 1
